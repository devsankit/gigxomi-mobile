name: Create Production Builds

on:
  workflow_dispatch: {}

jobs:
  build_android:
    name: Build Android Production
    type: build
    params:
      platform: android
      profile: production
  build_ios:
    name: Build iOS Production
    type: build
    params:
      platform: ios
      profile: production
